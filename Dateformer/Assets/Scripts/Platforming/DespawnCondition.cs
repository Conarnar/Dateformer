using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DespawnCondition : MonoBehaviour
{
    [SerializeField] bool despawnIfLowestAffinity;
    [SerializeField] bool despawnIfMiddleAffinity;
    [SerializeField] bool despawnIfHighestAffinity;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
